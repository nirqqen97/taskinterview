import { Border, Nav } from "./Header.styled"


export const Header = ( ) => { 
  
    return ( 
        <Border>
            <Nav to='/streamers'>
            Streamers
            </Nav>
        </Border>
    )
}